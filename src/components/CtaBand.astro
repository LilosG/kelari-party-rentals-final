---
import Section from "./Section.astro";

export interface Props {
  title: string;
  subtitle?: string;
  primaryCta: { label: string; href: string };
  secondaryCta?: { label: string; href: string };
}

const { title, subtitle, primaryCta, secondaryCta } = Astro.props;
---

<Section bg="dark" spacing="featured" width="wide">
  <div class="cta-band-shell text-center">
    <div class="cta-band-glow" aria-hidden="true"></div>
    <div class="relative z-10 mx-auto max-w-4xl">
      <p class="cta-band-eyebrow">Ready to plan with confidence?</p>
      <h2 class="font-display text-balance text-[2.4rem] font-bold leading-[1.02] tracking-[-0.025em] text-white md:text-[3.2rem] lg:text-[4rem]">
        {title}
      </h2>
      {subtitle && (
        <p class="mx-auto mt-5 max-w-2xl text-base leading-relaxed text-neutral-200 md:text-lg lg:text-[1.15rem]">
          {subtitle}
        </p>
      )}
      <div class="mt-9 flex flex-wrap justify-center gap-4 lg:mt-11">
        <a href={primaryCta.href} class="btn-primary btn-primary-lg">
          {primaryCta.label}
        </a>
        {secondaryCta && (
          <a href={secondaryCta.href} class="btn-secondary-on-dark btn-secondary-lg">
            {secondaryCta.label}
          </a>
        )}
      </div>
    </div>
  </div>
</Section>
