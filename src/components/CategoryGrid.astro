---
import Section from "./Section.astro";
import SectionHeader from "./SectionHeader.astro";

export interface CategoryCard {
  name: string;
  description: string;
  href: string;
  image?: string;
  icon?: string;
  slug?: string;
}

export interface Props {
  categories: CategoryCard[];
  columns?: 2 | 3 | 4;
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  bg?: "white" | "light" | "dark" | "pink-light";
  padding?: "default" | "compact" | "featured" | "none";
}

const {
  categories,
  columns = 3,
  eyebrow,
  title,
  subtitle,
  bg = "light",
  padding = "default",
} = Astro.props;

const isDark = bg === "dark";

const categoryImages: Record<string, string> = {
  tables: "/images/gallery-tablescape.jpg",
  chairs: "/images/backyard-party.jpg",
  "tents-canopies": "/images/gallery-canopy.jpg",
  linens: "/images/baby-shower.jpg",
  tableware: "/images/gallery-tablescape.jpg",
  decorations: "/images/gallery-marquee.jpg",
  lighting: "/images/engagement-party.jpg",
};

function getCategoryImage(cat: CategoryCard): string {
  if (cat.image) return cat.image;
  const slug = cat.slug ?? cat.href.split("/").filter(Boolean).pop() ?? "";
  return categoryImages[slug] ?? "/images/hero-party.jpg";
}

const colClass = {
  2: "grid-cols-1 sm:grid-cols-2",
  3: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3",
  4: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4",
}[columns];
---

<Section {bg} spacing={padding} width="wide">
  {(title || eyebrow) && (
    <div class="mb-10 md:mb-14">
      <SectionHeader {eyebrow} title={title ?? ""} {subtitle} dark={isDark} width="wide" />
    </div>
  )}

  <div class={`grid gap-6 ${colClass}`}>
    {categories.map((cat) => (
      <a href={cat.href} class="card-surface card-link group">
        <div class="card-image-wrap">
          <img
            src={getCategoryImage(cat)}
            alt={cat.name}
            width="1200"
            height="900"
            class="card-image"
            loading="lazy"
          />
        </div>
        <div class="card-body">
          <h3 class:list={[
            "card-title text-lg group-hover:text-brand-pink transition-colors",
            isDark ? "text-white" : "",
          ]}>
            {cat.name}
          </h3>
          <p class:list={[
            "card-copy card-copy-compact flex-1",
            isDark ? "text-neutral-300" : "",
          ]}>
            {cat.description}
          </p>
          <span class="card-link-text">Browse {cat.name} <span aria-hidden="true">&rarr;</span></span>
        </div>
      </a>
    ))}
  </div>
</Section>
